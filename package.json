{
  "name": "hl-stats",
  "version": "0.1.0",
  "description": "Readable React rebuild of the Qwantify Hyperliquid analytics workspace.",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1 --port 4174",
    "pages:build": "bash scripts/build-pages.sh",
    "pages:dev": "npm run pages:build && wrangler pages dev .cloudflare/pages",
    "pages:deploy": "npm run pages:build && wrangler pages deploy .cloudflare/pages --project-name=hl-stats"
  },
  "type": "module",
  "dependencies": {
    "lucide-react": "^0.577.0",
    "react": "^19.2.4",
    "react-dom": "^19.2.4",
    "react-router-dom": "^7.13.1",
    "recharts": "^3.8.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^6.0.1",
    "vite": "^8.0.1",
    "wrangler": "^4.80.0"
  }
}
